# ──────────────────────────────────────────────────────────────
# LegallyBound — requirements.txt
# RTX 4060 Laptop / CUDA 12.1 / Windows 11
# ──────────────────────────────────────────────────────────────

# 1. Install PyTorch with CUDA 12.1 support FIRST (separate command):
#    pip install torch==2.2.0+cu121 torchvision==0.17.0+cu121 torchaudio==2.2.0+cu121 \
#        --index-url https://download.pytorch.org/whl/cu121

# 2. Then install the rest:
transformers>=4.40.0
peft>=0.10.0
bitsandbytes>=0.43.0
accelerate>=0.28.0
trl>=0.8.0
datasets>=2.18.0
sentence-transformers>=2.7.0
duckdb>=0.10.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
streamlit>=1.33.0
rouge-score>=0.1.2
bert-score>=0.3.13
evaluate>=0.4.1
pandas>=2.2.0
tqdm>=4.66.0
scipy>=1.13.0
numpy<2.0
requests>=2.31.0
PyMuPDF>=1.24.0